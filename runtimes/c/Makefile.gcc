CFLAGS := -pipe -Wall -Werror -march=native -m64 -msse2 -mfpmath=sse \
          -fstrict-aliasing -ffast-math -O3 # -DNILE_DISABLE_THREADS

libnile.a: nile.o
	ar rcs $@ $^

nile.o: *.h

clean:
	$(RM) *.o *.a *.gch
