CFLAGS := -pipe -Wall -Werror -march=nocona -msse2 -mfpmath=sse -ffast-math -O3 #-g #-DNILE_DISABLE_THREADS -Wno-unused -m64

libnile.a: nile.o
	ar rcs $@ $^

nile.o: *.h

clean:
	$(RM) *.o *.a *.gch
